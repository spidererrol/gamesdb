import { GeneralProps } from "../props/GeneralProps"
import { IPager } from "./IPager"

interface PProps extends GeneralProps {
    pager: IPager
}

function Pager(props: PProps) {
    return <>TODO:Pager</>;
}

export default Pager;